version: '3.7'

services:
  web:
    build: ./project
    command: ./run
    volumes:
      - ./project:/usr/src/app
    ports:
      - 8002:8000
    environment:
      - ENVIRONMENT=dev
      - TESTING=0
